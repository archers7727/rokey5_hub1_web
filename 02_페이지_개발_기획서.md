# 두산 로봇팔 재료 손질 시스템 - 페이지 개발 기획서

## 🎨 UI/UX 공통 가이드라인

### 디자인 시스템

#### 컬러 팔레트
```css
/* Primary Colors */
--primary-blue: #0066CC;      /* 두산 브랜드 컬러 */
--primary-blue-dark: #004C99;
--primary-blue-light: #3385D6;

/* Status Colors */
--status-normal: #4CAF50;     /* 정상 작동 */
--status-warning: #FF9800;    /* 경고, 사용자 정지 */
--status-danger: #F44336;     /* 에러, 비상 정지 */
--status-idle: #9E9E9E;       /* 대기 중 */

/* Neutral Colors */
--gray-50: #FAFAFA;
--gray-100: #F5F5F5;
--gray-200: #EEEEEE;
--gray-300: #E0E0E0;
--gray-400: #BDBDBD;
--gray-500: #9E9E9E;
--gray-600: #757575;
--gray-700: #616161;
--gray-800: #424242;
--gray-900: #212121;

/* Background */
--bg-primary: #FFFFFF;
--bg-secondary: #F5F7FA;
--bg-dark: #1A1A1A;
```

#### 타이포그래피
```css
/* Font Family */
--font-primary: 'Pretendard', -apple-system, sans-serif;
--font-mono: 'JetBrains Mono', monospace;

/* Font Sizes */
--text-xs: 12px;    /* 작은 라벨 */
--text-sm: 14px;    /* 본문, 라벨 */
--text-base: 16px;  /* 기본 본문 */
--text-lg: 18px;    /* 중요 정보 */
--text-xl: 24px;    /* 섹션 제목 */
--text-2xl: 32px;   /* 페이지 제목 */
--text-3xl: 48px;   /* 히어로 타이틀 */

/* Font Weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

#### 스페이싱
```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-6: 24px;
--space-8: 32px;
--space-12: 48px;
--space-16: 64px;
```

#### Border Radius
```css
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-xl: 16px;
--radius-full: 9999px;
```

#### 그림자
```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.15);
```

### 반응형 브레이크포인트
```css
/* Mobile First Approach */
--breakpoint-sm: 640px;   /* 모바일 */
--breakpoint-md: 768px;   /* 태블릿 */
--breakpoint-lg: 1024px;  /* 데스크톱 */
--breakpoint-xl: 1280px;  /* 대형 데스크톱 */
--breakpoint-2xl: 1536px; /* 초대형 */
```

### 애니메이션
```css
/* Duration */
--duration-fast: 150ms;
--duration-normal: 300ms;
--duration-slow: 500ms;

/* Easing */
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

---

## 📄 MCA-01: 메인 대시보드

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ [네비게이션 바]                              [설정] │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────────────────┐  ┌─────────────────┐ │
│  │   로봇 상태 카드          │  │  오늘의 통계    │ │
│  │   ● 정상 작동 중          │  │  작업: 5건      │ │
│  │   M1013 연결됨           │  │  시간: 1시간   │ │
│  └──────────────────────────┘  └─────────────────┘ │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │         [🚀 작업 시작하기]                    │ │
│  │           (큰 CTA 버튼)                       │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  최근 작업 이력                                    │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │
│  │ 양파 썰기    │ │ 감자 튀김   │ │ 양파 튀김   │ │
│  │ 10분 전     │ │ 1시간 전   │ │ 2시간 전   │ │
│  │ [재실행]    │ │ [재실행]   │ │ [재실행]   │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<MainDashboard>
  <NavigationBar />
  
  <DashboardGrid>
    <RobotStatusCard 
      status={robotStatus}
      model="M1013"
      isConnected={true}
    />
    
    <TodayStatsCard 
      jobCount={5}
      totalTime={3600}
    />
    
    <StartJobButton 
      onClick={() => navigate('/job/new/material')}
    />
    
    <RecentJobsSection>
      {recentJobs.map(job => (
        <JobHistoryCard 
          key={job.id}
          job={job}
          onRerun={() => handleRerun(job)}
        />
      ))}
    </RecentJobsSection>
  </DashboardGrid>
</MainDashboard>
```

### 컴포넌트 명세

#### RobotStatusCard
```typescript
interface RobotStatusCardProps {
  status: 'idle' | 'working' | 'emergency_stop' | 'user_stop' | 'error';
  model: string;
  isConnected: boolean;
}

// 상태별 스타일
const statusStyles = {
  idle: { color: 'gray', icon: '○', text: '대기 중' },
  working: { color: 'green', icon: '●', text: '작동 중' },
  emergency_stop: { color: 'red', icon: '■', text: '비상 정지' },
  user_stop: { color: 'orange', icon: '▐▐', text: '일시 정지' },
  error: { color: 'red', icon: '✕', text: '에러' }
};
```

#### JobHistoryCard
```typescript
interface JobHistoryCardProps {
  job: {
    id: string;
    material: string;
    mode: string;
    completedAt: Date;
    duration: number;
  };
  onRerun: () => void;
}

// 상호작용
- 카드 전체 클릭: 상세 정보 모달
- [재실행] 버튼: 동일 설정으로 MCA-02-04로 이동
- 호버: elevation 증가, scale(1.02)
```

### 상태 관리
```typescript
// 전역 상태
const dashboardState = {
  robotStatus: 'idle' | 'working' | 'emergency_stop' | 'user_stop' | 'error',
  robotConnection: boolean,
  todayStats: {
    jobCount: number,
    totalTime: number, // seconds
    successRate: number
  },
  recentJobs: Job[] // 최근 3개
};

// 실시간 업데이트 (WebSocket)
useEffect(() => {
  const ws = new WebSocket('ws://localhost:8000/ws/robot-status');
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    setRobotStatus(data.status);
  };
}, []);
```

### 반응형 디자인
```css
/* Mobile (< 768px) */
.dashboard-grid {
  grid-template-columns: 1fr;
  gap: 16px;
}

/* Tablet (768px ~ 1024px) */
@media (min-width: 768px) {
  .dashboard-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
}

/* Desktop (>= 1024px) */
@media (min-width: 1024px) {
  .dashboard-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  
  .start-job-button {
    grid-column: 1 / -1;
  }
}
```

---

## 📄 MCA-02-01: 재료 선택

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ [← 뒤로]          재료 선택          [진행률 1/4]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│   어떤 재료를 손질하시겠어요?                        │
│                                                     │
│   ┌──────────────────┐    ┌──────────────────┐    │
│   │                  │    │                  │    │
│   │     [🧅]         │    │     [🥔]         │    │
│   │                  │    │                  │    │
│   │      양파         │    │      감자         │    │
│   │                  │    │                  │    │
│   │   신선한 양파로    │    │  고소한 감자로   │    │
│   │   다양한 요리를    │    │  튀김 요리를     │    │
│   │                  │    │                  │    │
│   └──────────────────┘    └──────────────────┘    │
│           ✓                                        │
│                                                     │
│                                                     │
│  [선택된 재료: 양파]                                │
│                                        [다음 →]    │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<MaterialSelection>
  <PageHeader>
    <BackButton />
    <Title>재료 선택</Title>
    <ProgressIndicator current={1} total={4} />
  </PageHeader>
  
  <PageContent>
    <SectionTitle>어떤 재료를 손질하시겠어요?</SectionTitle>
    
    <MaterialGrid>
      {materials.map(material => (
        <MaterialCard
          key={material.id}
          material={material}
          isSelected={selectedMaterial?.id === material.id}
          onClick={() => setSelectedMaterial(material)}
        />
      ))}
    </MaterialGrid>
  </PageContent>
  
  <FloatingFooter>
    <SelectedMaterialChip material={selectedMaterial} />
    <NextButton 
      disabled={!selectedMaterial}
      onClick={() => navigate('/job/new/mode')}
    />
  </FloatingFooter>
</MaterialSelection>
```

### 컴포넌트 명세

#### MaterialCard
```typescript
interface MaterialCardProps {
  material: {
    id: string;
    name: string;
    emoji: string;
    description: string;
    image?: string;
  };
  isSelected: boolean;
  onClick: () => void;
}

// 스타일링
const cardStyles = {
  base: {
    padding: '24px',
    borderRadius: '12px',
    border: '2px solid transparent',
    cursor: 'pointer',
    transition: 'all 300ms ease',
  },
  normal: {
    backgroundColor: 'white',
    borderColor: '#E0E0E0',
  },
  selected: {
    backgroundColor: '#E3F2FD',
    borderColor: '#0066CC',
    transform: 'scale(1.05)',
    boxShadow: '0 8px 16px rgba(0, 102, 204, 0.2)',
  },
  hover: {
    transform: 'translateY(-4px)',
    boxShadow: '0 12px 24px rgba(0, 0, 0, 0.1)',
  }
};
```

#### 인터랙션
```typescript
// 선택 상태 토글
const handleMaterialClick = (material: Material) => {
  setSelectedMaterial(material);
  
  // 햅틱 피드백 (모바일)
  if (navigator.vibrate) {
    navigator.vibrate(10);
  }
  
  // 선택 애니메이션
  animate(cardRef.current, {
    scale: [1, 1.1, 1],
  }, {
    duration: 0.3
  });
};

// 다음 버튼 활성화
const isNextEnabled = selectedMaterial !== null;
```

### 상태 관리
```typescript
// 로컬 상태
const [selectedMaterial, setSelectedMaterial] = useState<Material | null>(null);

// 전역 상태 (작업 설정 플로우)
const jobConfigStore = create((set) => ({
  material: null,
  mode: null,
  parameters: null,
  
  setMaterial: (material) => set({ material }),
  reset: () => set({ material: null, mode: null, parameters: null })
}));
```

### 예외 처리
- 재료 데이터 로딩 실패 → 에러 메시지 표시 + 재시도 버튼
- 네트워크 끊김 → 로컬 캐시 데이터 사용
- 다음 버튼 클릭 시 선택 안 됨 → 재료 카드에 진동 애니메이션

---

## 📄 MCA-02-02: 손질 모드 선택

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ [← 뒤로]        손질 모드 선택        [진행률 2/4]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  선택한 재료: 🧅 양파                                │
│                                                     │
│  어떻게 손질하시겠어요?                              │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │                                            │   │
│  │         [🍟 튀김 모드]                     │   │
│  │                                            │   │
│  │     재료를 균등한 조각으로 나눕니다          │   │
│  │                                            │   │
│  │     예상 시간: 약 2분                       │   │
│  │                                            │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │                                            │   │
│  │         [🔪 썰기 모드]                     │   │
│  │                                            │   │
│  │     원하는 두께로 슬라이스합니다            │   │
│  │                                            │   │
│  │     예상 시간: 약 3분                       │   │
│  │                                            │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│                                        [다음 →]    │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<ModeSelection>
  <PageHeader>
    <BackButton />
    <Title>손질 모드 선택</Title>
    <ProgressIndicator current={2} total={4} />
  </PageHeader>
  
  <PageContent>
    <SelectedMaterialChip material={jobConfig.material} />
    
    <SectionTitle>어떻게 손질하시겠어요?</SectionTitle>
    
    <ModeList>
      <ModeCard
        mode="frying"
        icon="🍟"
        title="튀김 모드"
        description="재료를 균등한 조각으로 나눕니다"
        estimatedTime={120}
        isSelected={selectedMode === 'frying'}
        onClick={() => setSelectedMode('frying')}
      />
      
      <ModeCard
        mode="slicing"
        icon="🔪"
        title="썰기 모드"
        description="원하는 두께로 슬라이스합니다"
        estimatedTime={180}
        isSelected={selectedMode === 'slicing'}
        onClick={() => setSelectedMode('slicing')}
      />
    </ModeList>
  </PageContent>
  
  <FloatingFooter>
    <NextButton 
      disabled={!selectedMode}
      onClick={() => navigate('/job/new/parameters')}
    />
  </FloatingFooter>
</ModeSelection>
```

### 컴포넌트 명세

#### ModeCard
```typescript
interface ModeCardProps {
  mode: 'frying' | 'slicing';
  icon: string;
  title: string;
  description: string;
  estimatedTime: number; // seconds
  isSelected: boolean;
  onClick: () => void;
}

// 큰 카드 스타일 (전체 폭)
const modeCardStyles = {
  width: '100%',
  padding: '32px',
  minHeight: '160px',
  display: 'flex',
  flexDirection: 'column',
  gap: '16px',
  borderRadius: '16px',
  border: '3px solid transparent',
  transition: 'all 300ms ease',
};
```

---

## 📄 MCA-02-03: 작업 파라미터 설정

### 레이아웃 구조 (튀김 모드)
```
┌─────────────────────────────────────────────────────┐
│ [← 뒤로]      작업 파라미터 설정      [진행률 3/4]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🧅 양파 > 🍟 튀김 모드                              │
│                                                     │
│  ┌─ 재료 크기 ─────────────────────────────────┐   │
│  │ ○ 작음 (6cm)  ● 보통 (8cm)  ○ 큼 (10cm)    │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 조각 수 ───────────────────────────────────┐   │
│  │  [선택: 4조각 ▼]                            │   │
│  │     - 2조각                                 │   │
│  │     - 4조각  ✓                              │   │
│  │     - 6조각                                 │   │
│  │     - 8조각                                 │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 반복 횟수 ──────────────────────────────────┐  │
│  │  [-] [ 3 ] [+]                              │   │
│  │  같은 작업을 3번 반복합니다                  │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 예상 정보 ──────────────────────────────────┐  │
│  │  • 예상 작업 횟수: 2번 절단                  │   │
│  │  • 단일 작업 시간: 약 2분                   │   │
│  │  • 총 작업 시간: 약 6분                     │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│                                        [다음 →]    │
└─────────────────────────────────────────────────────┘
```

### 레이아웃 구조 (썰기 모드)
```
┌─────────────────────────────────────────────────────┐
│ [← 뒤로]      작업 파라미터 설정      [진행률 3/4]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🧅 양파 > 🔪 썰기 모드                              │
│                                                     │
│  ┌─ 재료 크기 ─────────────────────────────────┐   │
│  │ ○ 작음 (6cm)  ● 보통 (8cm)  ○ 큼 (10cm)    │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 두께 선택 ──────────────────────────────────┐  │
│  │  5mm ──────●────────────────── 30mm        │   │
│  │                                             │   │
│  │  [ 5 | 10 | 15 | 20 | 25 | 30 ] mm        │   │
│  │         현재: 10mm                          │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 반복 횟수 ──────────────────────────────────┐  │
│  │  [-] [ 1 ] [+]                              │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  ┌─ 예상 정보 ──────────────────────────────────┐  │
│  │  • 예상 슬라이스: 8개                       │   │
│  │  • 단일 작업 시간: 약 3분                   │   │
│  │  • 총 작업 시간: 약 3분                     │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│                                        [다음 →]    │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<ParameterConfiguration>
  <PageHeader>
    <BackButton />
    <Title>작업 파라미터 설정</Title>
    <ProgressIndicator current={3} total={4} />
  </PageHeader>
  
  <PageContent>
    <BreadcrumbChip>
      {jobConfig.material.emoji} {jobConfig.material.name} 
      > {jobConfig.mode.icon} {jobConfig.mode.name}
    </BreadcrumbChip>
    
    {/* 공통 파라미터 */}
    <ParameterSection title="재료 크기">
      <RadioGroup
        value={materialSize}
        onChange={setMaterialSize}
        options={[
          { value: 'small', label: '작음', size: getSizeForMaterial('small') },
          { value: 'medium', label: '보통', size: getSizeForMaterial('medium') },
          { value: 'large', label: '큼', size: getSizeForMaterial('large') }
        ]}
      />
    </ParameterSection>
    
    {/* 모드별 파라미터 */}
    {jobConfig.mode.type === 'frying' && (
      <ParameterSection title="조각 수">
        <Dropdown
          value={pieces}
          onChange={setPieces}
          options={[2, 4, 6, 8]}
        />
      </ParameterSection>
    )}
    
    {jobConfig.mode.type === 'slicing' && (
      <ParameterSection title="두께 선택">
        <ThicknessSlider
          value={thickness}
          onChange={setThickness}
          min={5}
          max={30}
          step={5}
          unit="mm"
        />
      </ParameterSection>
    )}
    
    <ParameterSection title="반복 횟수">
      <NumberInput
        value={repeatCount}
        onChange={setRepeatCount}
        min={1}
        max={10}
      />
    </ParameterSection>
    
    <EstimationCard>
      <EstimationItem 
        label="예상 작업 횟수" 
        value={estimatedCuts} 
      />
      <EstimationItem 
        label="단일 작업 시간" 
        value={formatTime(singleJobTime)} 
      />
      <EstimationItem 
        label="총 작업 시간" 
        value={formatTime(totalJobTime)} 
      />
    </EstimationCard>
  </PageContent>
  
  <FloatingFooter>
    <NextButton 
      onClick={() => navigate('/job/new/confirm')}
    />
  </FloatingFooter>
</ParameterConfiguration>
```

### 컴포넌트 명세

#### ThicknessSlider
```typescript
interface ThicknessSliderProps {
  value: number;
  onChange: (value: number) => void;
  min: number;
  max: number;
  step: number;
  unit: string;
}

// 인터랙션
- 드래그로 값 조정
- 하단 버튼 클릭으로 정확한 값 선택
- 실시간 예상 정보 업데이트
```

#### NumberInput
```typescript
interface NumberInputProps {
  value: number;
  onChange: (value: number) => void;
  min: number;
  max: number;
}

// [-] [value] [+] 형태
// 버튼 클릭 시 애니메이션
// 범위 제한 (min ~ max)
```

### 계산 로직
```typescript
// 예상 작업 횟수 계산
const calculateEstimatedCuts = () => {
  if (mode === 'frying') {
    const cutMap = { 2: 1, 4: 2, 6: 3, 8: 3 };
    return cutMap[pieces];
  } else {
    const materialMm = MATERIAL_SIZES[material][materialSize] * 10;
    return Math.floor(materialMm / thickness);
  }
};

// 작업 시간 계산
const calculateJobTime = () => {
  const baseTime = mode === 'frying' ? 120 : 180; // seconds
  const cutsMultiplier = estimatedCuts * 0.5;
  return baseTime + cutsMultiplier;
};

// 실시간 업데이트
useEffect(() => {
  const cuts = calculateEstimatedCuts();
  const time = calculateJobTime();
  const total = time * repeatCount;
  
  setEstimatedCuts(cuts);
  setSingleJobTime(time);
  setTotalJobTime(total);
}, [materialSize, pieces, thickness, repeatCount]);
```

---

## 📄 MCA-02-04: 작업 확인 및 미리보기

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ [← 뒤로]          작업 확인          [진행률 4/4]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─ 작업 요약 ──────────┐  ┌─ 3D 미리보기 ──────┐ │
│  │                      │  │                    │ │
│  │ 재료: 🧅 양파        │  │  [로봇 애니메이션]  │ │
│  │ 모드: 🍟 튀김 모드   │  │                    │ │
│  │ 크기: 보통 (8cm)    │  │   [▶ 재생]         │ │
│  │ 조각: 4조각         │  │   [⏸ 일시정지]     │ │
│  │ 반복: 3회           │  │                    │ │
│  │                      │  │  [상단] [측면]     │ │
│  │ 예상 절단: 2번      │  │  [자유]           │ │
│  │ 예상 시간: 6분      │  │                    │ │
│  │                      │  └────────────────────┘ │
│  │ [💾 프리셋 저장]    │                          │
│  └──────────────────────┘                          │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │         [🔵 즉시 실행]                     │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<JobConfirmation>
  <PageHeader>
    <BackButton />
    <Title>작업 확인</Title>
    <ProgressIndicator current={4} total={4} />
  </PageHeader>
  
  <PageContent>
    <TwoColumnLayout>
      {/* 좌측: 작업 요약 */}
      <JobSummaryCard>
        <SummaryItem label="재료" value={`${material.emoji} ${material.name}`} />
        <SummaryItem label="모드" value={`${mode.icon} ${mode.name}`} />
        <SummaryItem label="크기" value={`${sizeLabel} (${sizeCm}cm)`} />
        
        {mode.type === 'frying' && (
          <SummaryItem label="조각" value={`${pieces}조각`} />
        )}
        
        {mode.type === 'slicing' && (
          <SummaryItem label="두께" value={`${thickness}mm`} />
        )}
        
        <SummaryItem label="반복" value={`${repeatCount}회`} />
        
        <Divider />
        
        <SummaryItem 
          label="예상 절단" 
          value={`${estimatedCuts}번`} 
          highlight 
        />
        <SummaryItem 
          label="예상 시간" 
          value={formatTime(totalJobTime)} 
          highlight 
        />
        
        <SavePresetButton onClick={handleSavePreset} />
      </JobSummaryCard>
      
      {/* 우측: 3D 미리보기 */}
      <Preview3DCard>
        <Canvas>
          <RobotSimulation 
            jobConfig={jobConfig}
            isPlaying={isPreviewPlaying}
            onComplete={() => setIsPreviewPlaying(false)}
          />
        </Canvas>
        
        <PreviewControls>
          <PlayPauseButton 
            isPlaying={isPreviewPlaying}
            onClick={() => setIsPreviewPlaying(!isPreviewPlaying)}
          />
          
          <CameraPresets>
            <CameraButton preset="top" label="상단" />
            <CameraButton preset="side" label="측면" />
            <CameraButton preset="free" label="자유" />
          </CameraPresets>
        </PreviewControls>
      </Preview3DCard>
    </TwoColumnLayout>
  </PageContent>
  
  <FloatingFooter>
    <ExecuteButton 
      variant="primary"
      size="large"
      onClick={handleExecute}
    >
      🔵 즉시 실행
    </ExecuteButton>
  </FloatingFooter>
</JobConfirmation>
```

### Three.js 미리보기 구현

#### RobotSimulation 컴포넌트
```typescript
interface RobotSimulationProps {
  jobConfig: JobConfig;
  isPlaying: boolean;
  onComplete: () => void;
}

function RobotSimulation({ jobConfig, isPlaying, onComplete }: RobotSimulationProps) {
  const robotRef = useRef<Group>();
  const [currentStep, setCurrentStep] = useState(0);
  const [progress, setProgress] = useState(0);
  
  // 작업 시퀀스 생성
  const workSequence = useMemo(() => 
    generateWorkSequence(jobConfig),
    [jobConfig]
  );
  
  // 애니메이션 루프
  useFrame((state, delta) => {
    if (!isPlaying || !robotRef.current) return;
    
    // 진행률 업데이트
    setProgress(prev => {
      const newProgress = prev + delta * 0.5; // 속도 조절
      
      if (newProgress >= 1) {
        // 다음 단계로
        const nextStep = currentStep + 1;
        if (nextStep >= workSequence.length) {
          onComplete();
          return 0;
        }
        setCurrentStep(nextStep);
        return 0;
      }
      
      return newProgress;
    });
    
    // 관절 각도 보간
    const currentPose = workSequence[currentStep];
    const nextPose = workSequence[currentStep + 1] || currentPose;
    
    applyInterpolatedPose(robotRef.current, currentPose, nextPose, progress);
  });
  
  return (
    <group ref={robotRef}>
      <RobotModel />
      <Workbench />
      <Material type={jobConfig.material.id} />
    </group>
  );
}
```

#### 작업 시퀀스 생성
```typescript
function generateWorkSequence(jobConfig: JobConfig): RobotPose[] {
  const { material, mode, parameters } = jobConfig;
  
  const sequence: RobotPose[] = [];
  
  // 1. 홈 포지션
  sequence.push({
    joints: [0, 0, 90, 0, 90, 0],
    duration: 1,
    action: 'idle'
  });
  
  // 2. 재료 위로 이동
  sequence.push({
    joints: [45, -30, 60, 0, 90, 0],
    duration: 2,
    action: 'move'
  });
  
  // 3. 작업 수행
  const cuts = mode.type === 'frying' 
    ? parameters.fryingParams.pieces
    : Math.floor((parameters.materialSize * 10) / parameters.slicingParams.thickness);
  
  for (let i = 0; i < cuts; i++) {
    // 절단 준비
    sequence.push({
      joints: [45 + i * 5, -30, 60, 0, 90, 0],
      duration: 1,
      action: 'prepare_cut'
    });
    
    // 절단 동작
    sequence.push({
      joints: [45 + i * 5, -30, 80, 0, 90, 0],
      duration: 0.5,
      action: 'cut'
    });
    
    // 복귀
    sequence.push({
      joints: [45 + i * 5, -30, 60, 0, 90, 0],
      duration: 0.5,
      action: 'retract'
    });
  }
  
  // 4. 홈 포지션 복귀
  sequence.push({
    joints: [0, 0, 90, 0, 90, 0],
    duration: 2,
    action: 'return_home'
  });
  
  return sequence;
}
```

### 프리셋 저장 기능
```typescript
const handleSavePreset = () => {
  const presetName = prompt('프리셋 이름을 입력하세요');
  if (!presetName) return;
  
  const preset: JobPreset = {
    id: generateId(),
    name: presetName,
    config: jobConfig,
    createdAt: new Date(),
    usageCount: 0
  };
  
  savePresetToStorage(preset);
  showToast('프리셋이 저장되었습니다', 'success');
};
```

---

## 📄 MCA-03: 작업 실행 모니터링

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│  작업 진행 중...  [⏸][⏹][🛑]    속도: [100%▼]     │
├─────────────────────────────────────────────────────┤
│  ████████████████░░░░░░  65%                       │
│  현재: 재료 절단 (2/3)  │  1:30 / 3:00            │
├───────────────────────┬─────────────────────────────┤
│                       │                             │
│   [3D 로봇 뷰]         │  로봇 상태                  │
│                       │  ━━━━━━━━━━━━━━━━━━━━━     │
│   실시간 로봇         │  현재 단계:                │
│   움직임 표시         │  "재료 절단 중"             │
│                       │                             │
│   [카메라]            │  관절 각도:                │
│   [상단][측면]        │  • J1: 45.2°               │
│   [자유]              │  • J2: -12.5°              │
│                       │  • J3: 60.8°               │
│                       │  • J4: 0.0°                │
│                       │  • J5: 90.0°               │
│                       │  • J6: 0.0°                │
│                       │                             │
│                       │  TCP 위치:                 │
│                       │  x: 0.45m                  │
│                       │  y: -0.12m                 │
│                       │  z: 0.68m                  │
│                       │                             │
│                       │  속도: 0.15 m/s            │
│                       │                             │
└───────────────────────┴─────────────────────────────┘
│  ├──1──┼──2──┼──3──┤  타임라인                     │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<JobMonitoring>
  <MonitoringHeader>
    <StatusText>작업 진행 중...</StatusText>
    
    <ControlButtons>
      <IconButton 
        icon={isPaused ? '▶' : '⏸'}
        onClick={handlePauseResume}
        tooltip={isPaused ? '재개' : '일시정지'}
      />
      <IconButton 
        icon='⏹'
        onClick={handleStop}
        tooltip='정지'
      />
      <IconButton 
        icon='🛑'
        onClick={handleEmergencyStop}
        variant="danger"
        tooltip='비상 정지'
      />
    </ControlButtons>
    
    <SpeedControl
      value={speed}
      onChange={setSpeed}
      options={[50, 75, 100, 125, 150]}
    />
  </MonitoringHeader>
  
  <ProgressBar 
    progress={progress}
    currentStep={currentStep}
    totalSteps={totalSteps}
    elapsedTime={elapsedTime}
    estimatedTime={estimatedTime}
  />
  
  <MonitoringContent>
    <Robot3DView>
      <Canvas>
        <RobotRealtime
          jointAngles={currentJointAngles}
          tcpPosition={tcpPosition}
        />
      </Canvas>
      
      <CameraControls>
        <CameraButton preset="top" />
        <CameraButton preset="side" />
        <CameraButton preset="free" />
      </CameraControls>
    </Robot3DView>
    
    <RobotStatusPanel>
      <StatusSection title="현재 단계">
        <StepDescription>{currentStepDescription}</StepDescription>
      </StatusSection>
      
      <StatusSection title="관절 각도">
        {jointAngles.map((angle, i) => (
          <JointDisplay 
            key={i}
            joint={i + 1}
            angle={angle}
          />
        ))}
      </StatusSection>
      
      <StatusSection title="TCP 위치">
        <CoordinateDisplay
          x={tcpPosition.x}
          y={tcpPosition.y}
          z={tcpPosition.z}
        />
      </StatusSection>
      
      <StatusSection title="속도">
        <SpeedDisplay value={currentSpeed} unit="m/s" />
      </StatusSection>
    </RobotStatusPanel>
  </MonitoringContent>
  
  <Timeline
    steps={jobSteps}
    currentStep={currentStep}
    onStepClick={handleJumpToStep}
  />
</JobMonitoring>
```

### WebSocket 실시간 통신
```typescript
// WebSocket 연결
useEffect(() => {
  const ws = new WebSocket(`ws://localhost:8000/ws/job/${jobId}`);
  
  ws.onopen = () => {
    console.log('WebSocket connected');
  };
  
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    
    // 상태 업데이트
    setProgress(data.progress);
    setCurrentStep(data.currentStep);
    setCurrentJointAngles(data.jointAngles);
    setTcpPosition(data.tcpPosition);
    setCurrentSpeed(data.speed);
    setCurrentStepDescription(data.stepDescription);
    
    // 완료 체크
    if (data.status === 'completed') {
      navigate(`/job/complete/${jobId}`);
    }
  };
  
  ws.onerror = (error) => {
    console.error('WebSocket error:', error);
    showToast('연결 오류가 발생했습니다', 'error');
  };
  
  ws.onclose = () => {
    console.log('WebSocket disconnected');
  };
  
  return () => ws.close();
}, [jobId]);
```

### 제어 함수
```typescript
// 일시정지/재개
const handlePauseResume = async () => {
  if (isPaused) {
    await api.post(`/api/jobs/${jobId}/resume`);
  } else {
    await api.post(`/api/jobs/${jobId}/pause`);
  }
  setIsPaused(!isPaused);
};

// 정지
const handleStop = async () => {
  if (confirm('작업을 정지하시겠습니까?')) {
    await api.post(`/api/jobs/${jobId}/stop`);
    navigate('/');
  }
};

// 비상 정지
const handleEmergencyStop = async () => {
  await api.post(`/api/robot/emergency-stop`);
  showToast('비상 정지되었습니다', 'warning');
  navigate('/robot/status');
};

// 속도 변경
const handleSpeedChange = async (newSpeed: number) => {
  await api.post(`/api/jobs/${jobId}/speed`, { speed: newSpeed });
  setSpeed(newSpeed);
};
```

---

## 📄 MCA-04: 작업 완료

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│                                                     │
│              ✅ 작업 완료!                          │
│                                                     │
│  ┌─ 작업 결과 ──────────────────────────────────┐  │
│  │                                               │  │
│  │  재료: 🧅 양파                                │  │
│  │  모드: 🍟 튀김 모드                           │  │
│  │  조각: 4조각                                  │  │
│  │                                               │  │
│  │  작업 시간: 5분 45초                          │  │
│  │  상태: ✓ 정상 완료                            │  │
│  │                                               │  │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │  │
│  │                                               │  │
│  │  💡 예상 시간(6분)보다 15초 빨랐습니다        │  │
│  │                                               │  │
│  └───────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────┐  ┌──────────────────┐       │
│  │                  │  │                  │       │
│  │  [🔄 같은 작업]  │  │  [➕ 새 작업]   │       │
│  │    반복하기       │  │    시작하기      │       │
│  │                  │  │                  │       │
│  └──────────────────┘  └──────────────────┘       │
│                                                     │
│               [🏠 메인으로]                         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<JobCompletion>
  <CompletionAnimation>
    <CheckmarkAnimation />
    <Title>작업 완료!</Title>
  </CompletionAnimation>
  
  <ResultCard>
    <ResultSection title="작업 결과">
      <ResultItem label="재료" value={job.material} />
      <ResultItem label="모드" value={job.mode} />
      {/* ... 기타 정보 */}
      
      <Divider />
      
      <ResultItem 
        label="작업 시간" 
        value={formatTime(job.duration)}
        highlight
      />
      <ResultItem 
        label="상태" 
        value="✓ 정상 완료"
        status="success"
      />
    </ResultSection>
    
    {/* 비교 통계 */}
    <ComparisonInsight>
      {job.duration < job.estimatedTime ? (
        <PositiveInsight>
          💡 예상 시간보다 {formatTime(job.estimatedTime - job.duration)} 빨랐습니다
        </PositiveInsight>
      ) : (
        <NeutralInsight>
          예상 시간과 유사하게 완료되었습니다
        </NeutralInsight>
      )}
    </ComparisonInsight>
  </ResultCard>
  
  <ActionButtons>
    <ActionButton
      variant="secondary"
      icon="🔄"
      label="같은 작업 반복하기"
      onClick={handleRepeat}
    />
    <ActionButton
      variant="primary"
      icon="➕"
      label="새 작업 시작하기"
      onClick={handleNewJob}
    />
  </ActionButtons>
  
  <BackToHomeButton onClick={() => navigate('/')}>
    🏠 메인으로
  </BackToHomeButton>
</JobCompletion>
```

### 애니메이션
```typescript
// 체크마크 애니메이션
const CheckmarkAnimation = () => {
  return (
    <motion.div
      initial={{ scale: 0, rotate: 0 }}
      animate={{ scale: 1, rotate: 360 }}
      transition={{
        type: 'spring',
        stiffness: 260,
        damping: 20
      }}
    >
      <CheckIcon size={80} color="green" />
    </motion.div>
  );
};

// Confetti 효과 (옵션)
useEffect(() => {
  confetti({
    particleCount: 100,
    spread: 70,
    origin: { y: 0.6 }
  });
}, []);
```

---

## 📄 MCA-05: 로봇 상태 페이지

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│              로봇 상태 모니터                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─ 로봇 전체 상태 ────────────────────────────┐   │
│  │                                             │   │
│  │  ● 정상 작동 중                             │   │
│  │  Doosan M1013  ✓ 연결됨                    │   │
│  │                                             │   │
│  │  [🛑 비상 정지]                             │   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────┬──────────────────────────┐ │
│  │                    │  관절 상세 정보           │ │
│  │   [3D 로봇 뷰]      │  ━━━━━━━━━━━━━━━━━━━━━ │ │
│  │                    │                          │ │
│  │   현재 자세 표시    │  Joint 1:  45.2°  [●●●] │ │
│  │                    │  Joint 2: -12.5°  [●●○] │ │
│  │   관절별 하이라이트 │  Joint 3:  60.8°  [●●●] │ │
│  │                    │  Joint 4:   0.0°  [○○○] │ │
│  │                    │  Joint 5:  90.0°  [●●●] │ │
│  │   [카메라 컨트롤]   │  Joint 6:   0.0°  [○○○] │ │
│  │                    │                          │ │
│  │                    │  TCP 정보:               │ │
│  │                    │  x:  0.450 m            │ │
│  │                    │  y: -0.120 m            │ │
│  │                    │  z:  0.680 m            │ │
│  │                    │                          │ │
│  │                    │  속도: 0.00 m/s         │ │
│  │                    │                          │ │
│  └────────────────────┴──────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 상태별 레이아웃 변화

#### 비상 정지 상태
```
┌─ 로봇 전체 상태 ──────────────────────────┐
│  🔴 비상 정지                             │
│  Doosan M1013  ✓ 연결됨                  │
│                                          │
│  [✓ 비상 정지 해제]                       │
└──────────────────────────────────────────┘
```

#### 사용자 정지 상태
```
┌─ 로봇 전체 상태 ──────────────────────────┐
│  🟡 사용자 정지                           │
│  Doosan M1013  ✓ 연결됨                  │
│  작업이 일시정지되었습니다                │
│                                          │
│  [▶ 재개]  [⏹ 완전 정지]                 │
└──────────────────────────────────────────┘
```

### 컴포넌트 구조
```typescript
<RobotStatusPage>
  <PageHeader>
    <Title>로봇 상태 모니터</Title>
  </PageHeader>
  
  <StatusCard>
    <OverallStatus
      status={robotStatus}
      model="M1013"
      isConnected={isConnected}
    />
    
    {/* 상태별 제어 버튼 */}
    {robotStatus === 'working' && (
      <EmergencyStopButton onClick={handleEmergencyStop} />
    )}
    
    {robotStatus === 'emergency_stop' && (
      <ReleaseEmergencyButton onClick={handleReleaseEmergency} />
    )}
    
    {robotStatus === 'user_stop' && (
      <StopControls>
        <ResumeButton onClick={handleResume} />
        <CompleteStopButton onClick={handleCompleteStop} />
      </StopControls>
    )}
  </StatusCard>
  
  <MonitoringGrid>
    <Robot3DPanel>
      <Canvas>
        <Robot3DModel
          jointAngles={currentJointAngles}
          highlightJoint={selectedJoint}
        />
      </Canvas>
      
      <CameraControls>
        <CameraButton preset="top" />
        <CameraButton preset="side" />
        <CameraButton preset="free" />
      </CameraControls>
    </Robot3DPanel>
    
    <JointDetailsPanel>
      <Section title="관절 상세 정보">
        {jointAngles.map((angle, i) => (
          <JointRow
            key={i}
            jointNumber={i + 1}
            angle={angle}
            load={jointLoads[i]}
            onHover={() => setSelectedJoint(i + 1)}
          />
        ))}
      </Section>
      
      <Section title="TCP 정보">
        <CoordinateRow label="x" value={tcpPosition.x} unit="m" />
        <CoordinateRow label="y" value={tcpPosition.y} unit="m" />
        <CoordinateRow label="z" value={tcpPosition.z} unit="m" />
        <CoordinateRow label="rx" value={tcpPosition.rx} unit="°" />
        <CoordinateRow label="ry" value={tcpPosition.ry} unit="°" />
        <CoordinateRow label="rz" value={tcpPosition.rz} unit="°" />
      </Section>
      
      <Section title="속도">
        <SpeedDisplay value={currentSpeed} unit="m/s" />
      </Section>
    </JointDetailsPanel>
  </MonitoringGrid>
</RobotStatusPage>
```

### 컴포넌트 명세

#### OverallStatus
```typescript
interface OverallStatusProps {
  status: RobotStatus;
  model: string;
  isConnected: boolean;
}

// 상태 표시 스타일
const statusConfig = {
  idle: {
    color: '#9E9E9E',
    icon: '○',
    text: '대기 중',
    bgColor: '#F5F5F5'
  },
  working: {
    color: '#4CAF50',
    icon: '●',
    text: '정상 작동 중',
    bgColor: '#E8F5E9'
  },
  emergency_stop: {
    color: '#F44336',
    icon: '■',
    text: '비상 정지',
    bgColor: '#FFEBEE'
  },
  user_stop: {
    color: '#FF9800',
    icon: '▐▐',
    text: '사용자 정지',
    bgColor: '#FFF3E0'
  },
  error: {
    color: '#F44336',
    icon: '✕',
    text: '에러',
    bgColor: '#FFEBEE'
  }
};
```

#### JointRow
```typescript
interface JointRowProps {
  jointNumber: number;
  angle: number;
  load?: number; // 0-100%
  onHover: () => void;
}

// 표시 예시
// Joint 1:  45.2°  [●●●○○]  (60% 부하)
//           ↑       ↑
//         각도   부하 바
```

### WebSocket 실시간 통신
```typescript
useEffect(() => {
  const ws = new WebSocket('ws://localhost:8000/ws/robot-status');
  
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    
    setRobotStatus(data.status);
    setCurrentJointAngles(data.jointAngles);
    setJointLoads(data.jointLoads);
    setTcpPosition(data.tcpPosition);
    setCurrentSpeed(data.speed);
  };
  
  return () => ws.close();
}, []);
```

### 제어 함수
```typescript
// 비상 정지
const handleEmergencyStop = async () => {
  if (confirm('비상 정지하시겠습니까?')) {
    await api.post('/api/robot/emergency-stop');
    showToast('비상 정지되었습니다', 'warning');
  }
};

// 비상 정지 해제
const handleReleaseEmergency = async () => {
  await api.post('/api/robot/release-emergency');
  showToast('비상 정지가 해제되었습니다', 'success');
};

// 재개
const handleResume = async () => {
  await api.post('/api/robot/resume');
  showToast('작업이 재개되었습니다', 'success');
};

// 완전 정지
const handleCompleteStop = async () => {
  if (confirm('작업을 완전히 정지하시겠습니까?')) {
    await api.post('/api/robot/stop');
    navigate('/');
  }
};
```

---

## 📄 MCA-06: 작업 경로 설정

### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│              작업 경로 설정                          │
├──────────┬────────────────────────┬─────────────────┤
│          │                        │                 │
│ 경로목록  │   [3D 작업 공간]       │  포인트 편집     │
│ ━━━━━━━ │                        │  ━━━━━━━━━━━━━ │
│          │   로봇 + 작업대         │                 │
│ 기본경로  │                        │  선택: Point 2  │
│ (잠금)   │   ● Point 1 (픽업)     │                 │
│          │   ● Point 2 (작업)     │  타입:          │
│ ▶ 경로1   │   ● Point 3 (배치)     │  [작업 포인트▼] │
│  경로2   │                        │                 │
│  경로3   │   [━━━━━━━━]          │  좌표:          │
│          │    경로선 표시          │  X: [0.450] m  │
│ [+ 추가] │                        │  Y: [0.120] m  │
│          │   [카메라 회전]         │  Z: [0.680] m  │
│          │                        │  RX: [0.0]  °  │
│          │   [⊕ 포인트 추가]       │  RY: [0.0]  °  │
│          │                        │  RZ: [0.0]  °  │
│          │                        │                 │
│          │                        │  [▲ 위로]       │
│          │                        │  [▼ 아래로]     │
│          │                        │  [🗑 삭제]      │
│          │                        │                 │
│          │  [▶ 시뮬레이션]         │  [💾 저장]      │
└──────────┴────────────────────────┴─────────────────┘
```

### 컴포넌트 구조
```typescript
<PathConfiguration>
  <PageHeader>
    <Title>작업 경로 설정</Title>
  </PageHeader>
  
  <ThreeColumnLayout>
    {/* 좌측: 경로 목록 */}
    <PathListPanel>
      <PathListItem
        name="기본 경로"
        isLocked={true}
        isSelected={selectedPath === 'default'}
        onClick={() => setSelectedPath('default')}
      />
      
      {userPaths.map(path => (
        <PathListItem
          key={path.id}
          name={path.name}
          isSelected={selectedPath === path.id}
          onClick={() => setSelectedPath(path.id)}
          onDelete={() => handleDeletePath(path.id)}
        />
      ))}
      
      <AddPathButton onClick={handleAddPath} />
    </PathListPanel>
    
    {/* 중앙: 3D 작업 공간 */}
    <WorkspacePanel>
      <Canvas
        camera={{ position: [2, 2, 2], fov: 50 }}
        onPointerDown={handleCanvasClick}
      >
        <Robot3DModel />
        <Workbench />
        
        {/* 포인트 표시 */}
        {pathPoints.map((point, i) => (
          <PointMarker
            key={i}
            position={[point.x, point.y, point.z]}
            type={point.type}
            isSelected={selectedPointIndex === i}
            onClick={() => setSelectedPointIndex(i)}
          />
        ))}
        
        {/* 경로선 */}
        <PathLine points={pathPoints} />
        
        <OrbitControls />
        <Environment preset="studio" />
      </Canvas>
      
      <WorkspaceControls>
        <AddPointButton onClick={() => setAddingPoint(true)} />
        <SimulationButton onClick={handleSimulation} />
      </WorkspaceControls>
    </WorkspacePanel>
    
    {/* 우측: 포인트 편집 */}
    <PointEditPanel>
      {selectedPoint && (
        <>
          <SectionTitle>선택: Point {selectedPointIndex + 1}</SectionTitle>
          
          <FormGroup label="타입">
            <Select
              value={selectedPoint.type}
              onChange={(type) => handleUpdatePointType(type)}
              options={[
                { value: 'pickup', label: '픽업 포인트' },
                { value: 'work', label: '작업 포인트' },
                { value: 'place', label: '배치 포인트' },
                { value: 'waypoint', label: '경유 포인트' }
              ]}
            />
          </FormGroup>
          
          <FormGroup label="좌표">
            <CoordinateInput
              label="X"
              value={selectedPoint.x}
              onChange={(v) => handleUpdateCoordinate('x', v)}
              unit="m"
            />
            <CoordinateInput
              label="Y"
              value={selectedPoint.y}
              onChange={(v) => handleUpdateCoordinate('y', v)}
              unit="m"
            />
            <CoordinateInput
              label="Z"
              value={selectedPoint.z}
              onChange={(v) => handleUpdateCoordinate('z', v)}
              unit="m"
            />
          </FormGroup>
          
          <FormGroup label="자세">
            <CoordinateInput
              label="RX"
              value={selectedPoint.rx}
              onChange={(v) => handleUpdateCoordinate('rx', v)}
              unit="°"
            />
            <CoordinateInput
              label="RY"
              value={selectedPoint.ry}
              onChange={(v) => handleUpdateCoordinate('ry', v)}
              unit="°"
            />
            <CoordinateInput
              label="RZ"
              value={selectedPoint.rz}
              onChange={(v) => handleUpdateCoordinate('rz', v)}
              unit="°"
            />
          </FormGroup>
          
          <PointActions>
            <IconButton 
              icon="▲" 
              onClick={handleMovePointUp}
              disabled={selectedPointIndex === 0}
            />
            <IconButton 
              icon="▼" 
              onClick={handleMovePointDown}
              disabled={selectedPointIndex === pathPoints.length - 1}
            />
            <IconButton 
              icon="🗑" 
              onClick={handleDeletePoint}
              variant="danger"
            />
          </PointActions>
        </>
      )}
      
      <SaveButton onClick={handleSavePath} />
    </PointEditPanel>
  </ThreeColumnLayout>
</PathConfiguration>
```

### 3D 인터랙션

#### 포인트 추가
```typescript
const handleCanvasClick = (event: ThreeEvent<MouseEvent>) => {
  if (!addingPoint) return;
  
  // 클릭 위치를 월드 좌표로 변환
  const worldPosition = event.point;
  
  // 새 포인트 추가
  const newPoint: PathPoint = {
    id: generateId(),
    type: 'waypoint',
    x: worldPosition.x,
    y: worldPosition.y,
    z: worldPosition.z,
    rx: 0,
    ry: 0,
    rz: 0
  };
  
  setPathPoints([...pathPoints, newPoint]);
  setAddingPoint(false);
  setSelectedPointIndex(pathPoints.length);
};
```

#### PointMarker 컴포넌트
```typescript
function PointMarker({ position, type, isSelected, onClick }: PointMarkerProps) {
  const colorMap = {
    pickup: '#2196F3',    // 파란색
    work: '#FFC107',      // 노란색
    place: '#4CAF50',     // 초록색
    waypoint: '#9E9E9E'   // 회색
  };
  
  return (
    <mesh
      position={position}
      onClick={onClick}
      scale={isSelected ? 1.5 : 1}
    >
      <sphereGeometry args={[0.05, 16, 16]} />
      <meshStandardMaterial 
        color={colorMap[type]}
        emissive={isSelected ? colorMap[type] : 'black'}
        emissiveIntensity={isSelected ? 0.5 : 0}
      />
    </mesh>
  );
}
```

#### PathLine 컴포넌트
```typescript
function PathLine({ points }: PathLineProps) {
  const linePoints = points.map(p => new Vector3(p.x, p.y, p.z));
  
  return (
    <Line
      points={linePoints}
      color="#0066CC"
      lineWidth={2}
      dashed={true}
      dashSize={0.05}
      gapSize={0.02}
    />
  );
}
```

### 경로 시뮬레이션
```typescript
const handleSimulation = async () => {
  setIsSimulating(true);
  
  // 경로 검증
  const validation = await validatePath(pathPoints);
  
  if (!validation.valid) {
    showToast(validation.error, 'error');
    setIsSimulating(false);
    return;
  }
  
  // 시뮬레이션 실행
  for (let i = 0; i < pathPoints.length - 1; i++) {
    await animateRobotMovement(
      pathPoints[i],
      pathPoints[i + 1]
    );
  }
  
  setIsSimulating(false);
  showToast('시뮬레이션 완료', 'success');
};

// 경로 검증
async function validatePath(points: PathPoint[]): Promise<ValidationResult> {
  // 최소 포인트 수 체크
  if (points.length < 2) {
    return { valid: false, error: '최소 2개 이상의 포인트가 필요합니다' };
  }
  
  // 충돌 체크
  const collisionCheck = await checkCollisions(points);
  if (collisionCheck.hasCollision) {
    return { valid: false, error: '경로에 충돌이 감지되었습니다' };
  }
  
  // 도달 가능 여부 체크
  for (const point of points) {
    const reachable = await checkReachability(point);
    if (!reachable) {
      return { valid: false, error: '도달할 수 없는 포인트가 있습니다' };
    }
  }
  
  return { valid: true };
}
```

### 저장 및 불러오기
```typescript
// 경로 저장
const handleSavePath = async () => {
  const pathData: PathData = {
    id: currentPathId || generateId(),
    name: pathName,
    points: pathPoints,
    createdAt: new Date(),
    updatedAt: new Date()
  };
  
  // LocalStorage에 저장 (향후 DB로 변경)
  const paths = JSON.parse(localStorage.getItem('robot-paths') || '[]');
  const index = paths.findIndex(p => p.id === pathData.id);
  
  if (index >= 0) {
    paths[index] = pathData;
  } else {
    paths.push(pathData);
  }
  
  localStorage.setItem('robot-paths', JSON.stringify(paths));
  showToast('경로가 저장되었습니다', 'success');
};

// 경로 불러오기
const loadPath = (pathId: string) => {
  const paths = JSON.parse(localStorage.getItem('robot-paths') || '[]');
  const path = paths.find(p => p.id === pathId);
  
  if (path) {
    setCurrentPathId(path.id);
    setPathName(path.name);
    setPathPoints(path.points);
  }
};
```

---

## 🎨 공통 UI 컴포넌트 라이브러리

### Button
```typescript
interface ButtonProps {
  variant: 'primary' | 'secondary' | 'danger' | 'ghost';
  size: 'sm' | 'md' | 'lg';
  icon?: ReactNode;
  loading?: boolean;
  disabled?: boolean;
  onClick: () => void;
  children: ReactNode;
}
```

### Card
```typescript
interface CardProps {
  title?: string;
  subtitle?: string;
  children: ReactNode;
  variant?: 'default' | 'outlined' | 'elevated';
}
```

### Modal
```typescript
interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title: string;
  children: ReactNode;
  actions?: ReactNode;
}
```

### Toast
```typescript
interface ToastProps {
  message: string;
  type: 'success' | 'error' | 'warning' | 'info';
  duration?: number;
}
```

---

**작성일**: 2025년 12월 12일  
**버전**: 2.0 (MCA-05 수정 반영)
